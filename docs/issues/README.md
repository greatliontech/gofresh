# Issues

Parked deferrals. Each entry carries a `Lands:` trigger; the doc is deleted
when its work lands (git holds history).

| issue | summary | Lands |
|---|---|---|
| [guidance-lint-zero-default-spelling](guidance-lint-zero-default-spelling.md) | the usage projection drops every `(default X)` form while the CLI lint refuses a spelled default only for a printed one — a derived default on a zero-registered flag vanishes from the served help unjudged | cross-tool train chunk 184 |
| [prints-default-one-home](prints-default-one-home.md) | the printed-default predicate the guidance CLI lint registers is a type switch copied into stipulator, gomutant, and pew; one fleet home over pflag's two strings | cross-tool train chunk 184 |
| [containment-over-a-consumer-command](containment-over-a-consumer-command.md) | gotool.Containment is applied by Runner alone, which prepares `go` only; gomutant, stipulator, and pew each carry a hand copy of the boundary for their non-go spawns | cross-tool train chunk 281 |
| [containment-windows-job-object](containment-windows-job-object.md) | gotool.Containment's Windows arm is a tree kill; gomutant's oracle keeps its own Job Object containment beside it | user decision |
| [refused-in-module-path-spelled-absolute](refused-in-module-path-spelled-absolute.md) | an in-module refused path's absolute spelling keeps the runtime-input identity checkout-local; the fork is a module-relative clause (a fleet re-key) or the status quo | user decision |
| [coverage-lint-default-on-zero-default-flag](coverage-lint-default-on-zero-default-flag.md) | a `(default X)` parenthetical on a zero-default flag is unjudged and its prose is dropped from the served usage | cross-tool train chunk 184 |
| [cli-long-help-rendering-two-homes](cli-long-help-rendering-two-homes.md) | the knobbed verb's long help join (help + pointer) is spelled by each consumer | cross-tool train chunk 184 |
| [sampler-failed-sample-memo-unpinned](sampler-failed-sample-memo-unpinned.md) | the sampler memoizes a failed sample; no pin counts one spawn for two asks after a failure | cross-tool train chunk 206 |
| [gotool-carries-no-env-drop](gotool-carries-no-env-drop.md) | the env policy has a setter and no drop; a consumer's denial re-spells the key rule | cross-tool train chunk 240 |
| [closure-sub-contract-granularity-residue](closure-sub-contract-granularity-residue.md) | three split closure paragraphs still bundle several independently editable rules | cross-tool train chunk 193 |
| [loader-spawns-outside-the-runner](loader-spawns-outside-the-runner.md) | the package loader's `go list` children spawn through `x/tools`, outside `gotool.Runner`'s hook: a consumer's process boundary covers the runner's children only | cross-tool train chunk 241 |
| [per-file-memos-one-shape](per-file-memos-one-shape.md) | the effect-scan, ledger-parse, and canonical-digest memos each spell the load-serve-pend-merge discipline; one generic per-file memo would hold it once | cross-tool train chunk 203 |
| [walk-dst-selection-for-audit-key](walk-dst-selection-for-audit-key.md) | the two-axis key landed (chunk 126): dst-tagged analyses refuse admissions loudly; the dst-selection WALK remains — walking it lists dst in the selections axis | with the first judged run over a dst-tagged build selection (tugboat DST-leg campaigns, paused with the tool phase) |
| [binary-roots-single-mask-union](binary-roots-single-mask-union.md) | one all-roots RTA walk instead of per-root batches for the binary inventory | with the next change to the attributed reach's mask assignment |
| [discharge-reason-channel-one-source](discharge-reason-channel-one-source.md) | the downgrade reason's verb literals and channel clause are composed at two points; one composition keyed by a channel enum the discharge machinery carries | cross-tool train chunk 202 (199 folded into it) |
| [unify-carrier-walks](unify-carrier-walks.md) | one carrier-type walk parameterized by TypeParam policy and leaf table; the two openness walks over signatures ride it | with the next change to the carrier rule itself |
| [grpc-runtime-memo-and-registry-discharges](grpc-runtime-memo-and-registry-discharges.md) | 19 content-invariant lazy memos (protobuf/hpack get-or-compute fills) plus the channelz registry class; the consumer's enumeration-backed purity waiver retires when this lands (previously unindexed — caught by the 2026-08-26 re-slot sweep) | memos with immutable-after-construction-objects; channelz user decision |
| [attestation-keyed-record](attestation-keyed-record.md) | collapse the per-mode discharge plumbing into one attestation-keyed record | when a third execution-model attestation is introduced |
| [reusable-ci-workflow](reusable-ci-workflow.md) | chunk-107 ci.yaml exists in four near-identical copies; the rc-resolution loudness contract must be fixed four times | user decision — deferred 2026-09-07 (an outward-facing act) |
| [registration-audit-walk-helper-unification](registration-audit-walk-helper-unification.md) | three break-walks + three literal-element walks in the registration audit; one helper each (chunk-83 review) | with unify-carrier-walks' collapse |
| [fixture-tier-parallelism](fixture-tier-parallelism.md) | the full tier is child-process-bound and serial; `t.Parallel()` needs a shared-state audit first (`t.Setenv` in 12 files, global memo roots) | when the full tier's measured wall crosses 15m, or the next change to the closure fixtures' shared state |
| [memo-scope-axes](memo-scope-axes.md) | the per-file memos are one shape written twice, five file-read paths digest the same bytes, and the dynamic-state result type serves two roles | cross-tool train chunk 203 |
| [attribution-mask-width](attribution-mask-width.md) | the attributed-RTA slice is one machine word wide; fewer slices measured faster; widening the mask is a memory-and-cancel-granularity trade | a measured campaign or check names a package above 64 subjects whose slice count dominates its wall time (no measured hotspot names it today) |
| [first-offender-refusals](first-offender-refusals.md) | Sibling and CheckBatch membership refusals name only the first offender, CheckBatch's nondeterministically; collapse onto the batch-naming construction refusal | a consumer narrows a batch by one of them |
| [audited-set-versions-decoupled](audited-set-versions-decoupled.md) | the audited-pure set, the effect-scan strategy, and the observation strategy are three edits nothing couples; one was missed at the math/big widening | the next widening of an audited set, or the next change to either strategy version |
| [net-url-parse-godebug-channel](net-url-parse-godebug-channel.md) | url.Parse and its siblings read GODEBUG, pinned by no code-result guard and moved by an in-process Setenv; the escaping half is admitted, the parsing half filed | cross-tool train chunk 191 |
| [harness-admission-families-one-table](harness-admission-families-one-table.md) | logging, subtest driver, fuzz driver, and pacing repeat six sites each; one table keyed by symbol name | with the next harness admission family |
| [dispatch-admissions-one-predicate](dispatch-admissions-one-predicate.md) | the walk grants the dispatch admission by two routes with different target clauses; one predicate for both arms | with invoke-targets-narrowed-by-operand |
| [fold-import-name-declared](fold-import-name-declared.md) | the file fold binds an unnamed import by its path, not its declared name; the major-version case is handled, dotted names are not | when a classified package's declared name differs from its path, or with the next maximal-tier change |
| [dependency-nested-closures-outside-the-drain](dependency-nested-closures-outside-the-drain.md) | the object drain scans a first-party function's nested closures but refuses dependency packages before that arm; whether the drain scans dependency bodies or consults a per-function effect summary is a design question | cross-tool train chunk 192 |
| [closed-value-ladders-one-source](closed-value-ladders-one-source.md) | the closed-value walk and the target collector are two arm ladders over one shape with an asymmetric safety direction nothing enforces; one ladder returning both answers | with dispatch-admissions-one-predicate's collapse, or the next arm added to either ladder |
| [invoke-targets-narrowed-by-operand](invoke-targets-narrowed-by-operand.md) | an interface invoke keeps the enumeration's whole projection while a computed call narrows to its operand's functions; the collector's MakeInterface arm yields the concrete types | a field report of an invoke-form refusal, or dispatch-admissions-one-predicate's collapse |
| [sync-variable-judgments-one-shape](sync-variable-judgments-one-shape.md) | the proven pool and the data memo walk a sync variable's uses with two completeness strategies and two receiver ladders; the audited-set admissions are five one-liners plus a mirror | with the next audited sync set, or flag-surface-one-table's collapse |
| [immutable-after-construction-objects](immutable-after-construction-objects.md) | a package-level pointer to an in-package struct whose methods are proven read-only and whose fields are written only at construction is object-closed; a fixpoint across the receiver, parameter-leak, and type-immutability judgments | cross-tool train chunk 193 |
| [direct-call-site-index-one-source](direct-call-site-index-one-source.md) | three purity walks each rebuild the direct-call-site index and spell the callee-unwrapping ladder; one index, one unwrap | with the next judgment needing call-site provenance, or flag-surface-one-table's collapse |
| [flag-surface-one-table](flag-surface-one-table.md) | package flag's admitted surface is answered by four tables and the fold/walk symbol ladder is written twice; one surface predicate, one ladder | with classb-arms-one-table's collapse, or a fifth flag surface site |
| [classb-arms-one-table](classb-arms-one-table.md) | nine per-package classifier arms beside a gate whose deletion no test distinguishes; one table keyed by package and symbol | with the next change to classBEffect's own arms |
| [runtime-inputs-dirty-unadopted](runtime-inputs-dirty-unadopted.md) | REQ-inputs-dirty specifies a CommitInspector no consumer implements; adopt in gomutant or retire the clause | user decision |
| [static-input-root-unadopted](static-input-root-unadopted.md) | WithStaticInputRoot is its clause's one acceptance with no caller across the four repos; adopt or retire. | user decision |
| [runtime-input-entries-implicit-context](runtime-input-entries-implicit-context.md) | Adopt/Absolute/Relative/FromTestLog/Merge/Describe/Paths run go commands under context.Background() internally; one form per entry on the context axis. | cross-tool train chunk 243 |
| [root-directory-input-with-empty-manifest](root-directory-input-with-empty-manifest.md) | every evidence subject of a pb campaign classifies `external directory input: /` with an empty input manifest, while a syscall trace touches no such path; the origin of the `/` is not carried | the next report of the reason, now attributed (259 landed the attribution) |
| [iterator-calls-and-std-invokes-open-the-closure](iterator-calls-and-std-invokes-open-the-closure.md) | a range over `strings.SplitSeq` is a computed call and a `crypto/sha256` hash's `Sum` an invoke outside RTA; both refuse the subject's observation | cross-tool train chunk 260 (the iterator half); the invoke half rides invoke-targets-narrowed-by-operand |
| [json-record-readers-one-home](json-record-readers-one-home.md) | the manifest and fingerprint decoders spell the same JSON-object primitives (unique keys, null, end of input, canonical bytes) twice with two refusal ladders; one internal reader under the caller's noun | cross-tool train chunk 243 |
| [bracket-move-attribution-unsplit](bracket-move-attribution-unsplit.md) | the observation-bracket refusal attributes with a bracketed member list that `RefusalClause` does not split — gomutant keeps a second strip | a gofresh change set amending REQ-inputs-refusal-attribution's governed set; gomutant's copy deletes at the bump behind it |
