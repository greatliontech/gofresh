# Observation completeness proof

- [x] 1.1 Run chunk-start triage, fold the observation-completeness issue into this plan, and identify the governing contract amendments and breaking decisions.
- [x] 1.2 Define the attributable assertion-plus-proof contract, versioned compatibility rules, complete admitted-effect set, suppressible runtime-evidence dispositions, child-termination requirement, and fail-closed path semantics; exclude metadata-only observations from the lift and record every not-yet-enforced property as a spec-tier invariant with its implementing chunk as `Lands:`.
- [x] 2. Refactor maximal and declaration-RTA external-effect analysis to collect complete typed facts while preserving current closure hashes, verdicts, and preferred diagnostics.
- [x] 3. Promote and enforce the runtime-evidence invariants by closing producer-log dispositions, requiring one termination- and outcome-gated observation per contributing process, and refusing path identities whose kernel resolution cannot be reproduced.
- [x] 4. Promote and enforce the read-only proof invariants, implement the caller-selected versioned declaration-RTA proof, lift closure unverifiability only when the assertion, proof, runtime evidence, and ordinary guards all hold, then promote surviving issue rationale and delete the resolved issue.
- [ ] 5. Promote and enforce the path-value invariants, then extend observability analysis with provenance for run-created paths, destructive operations, and `OpenFile` flags so supported temporary-file workflows can be proved without broad purity assertions.
- [ ] 6.1 Run close-out triage, promote surviving rationale, delete this plan, and verify the complete contract and adversarial test surface.
